export const getNumberOfPrescriptions = (prescriptions: any[]): number => {
    return prescriptions.length;
}

export const getNumberOfMedicalRecords = (medicalRecords: any[]): number => {
    return medicalRecords.length;
}

export const getNumberOfMedicalHistory = (medicalHistory: any[]): number => {
    return medicalHistory.length;
}